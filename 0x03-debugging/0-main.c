#include "main.h"
#include <stdio.h>
/**
 * main - tests function that prints integer if is positive or negative
 * Return: 0
 */

int main(void)
{
	int x;

	x = 0;
	positive_or_negative(x);

	return (0);
}
