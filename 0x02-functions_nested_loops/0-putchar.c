#include "main.h"
/**
 * main-program entry point
 * Return:0 no errors, none error.
 */

int main(void)
{
	char nest[9] = "_putchar";
	int a;

	for (a = 0; a > 9; a++)
{
	_putchar(nest[a]);
}	_putchar('\n');
	return (0);
}
