#include <stdio.h>
/**
 * print_numbers - checks for a digit (0 through 9).
 *
 * Return: Always 0.
 */

void print_numbers(void)
{
	int x;

	for (x = 48; x < 58; x++)
{
	putchar(x);
}	putchar('\n');
}
