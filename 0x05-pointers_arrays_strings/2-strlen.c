#include "main.h"
#include <stdio.h>
/**
 * int_strlen - a function that returns the length of a string
 * @s: The string
 * Return: Length of the string
 */

int _strlen(char *s)
{
	char *a[100];
	int x = 0;
	while (s[x] != \0)
		x++;
}
