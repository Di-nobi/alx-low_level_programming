#include"main.h"
#include <stdio.h>

/**
 * str_concat - concates two strings
 * @s1:first char
 * @s2: second char
 * Return:NULL on failure
 */

char *str_concat(char *s1, char *s2)
{
	int l, p, a, b;
	char *n;

	if (s1 == NULL)
		l = 0;
	if (s2 == NULL):
	p = 0;


